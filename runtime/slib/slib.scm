(module slib
   (from
    (format "slib/format.scm")
    (sort "slib/sort.scm")
    (soundex "slib/soundex.scm")
    (strcase "slib/strcase.scm")
    (strsrch "slib/strsrch.scm")))
;    (export
;     ;format.scm
;     cl-format
;     ;sort.scm
;     slib-sorted? 
;     slib-merge 
;     slib-merge!
;     slib-sort 
;     slib-sort!
;     ;soundex.scm
;     (remove-if pred? lst)
;     (soundex str)
;     do-SOUNDEX
;     ;strcase.scm
; ;    (string-upcase! str)
; ;    (string-upcase str)
; ;    (string-downcase! str)
; ;    (string-downcase str)
; ;    (string-capitalize! str)
; ;    (string-capitalize str)
;     string-ci->symbol
; ;    symbol-append
;     (StudlyCapsExpand nstr . delimitr)
;    ;strsrch.scm
;    (string-index str chr)
;    (string-index-ci str chr)
;    (string-reverse-index str chr)
;    (string-reverse-index-ci str chr)
;    (substring? pat str)
;    (substring-ci? pat str)
;    (subskip skip pat patlen str strlen char=)
;    (subloop pat patlen str strlen char=)
;    (find-string-from-port? str <input-port> . max-no-char)
;    (string-subst text old new . rest)
;    (count-newlines str) ))


